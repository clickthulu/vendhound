<svg width="{{ width ?? 30 }}" height="{{ height ?? 30 }}">
    <circle cx="{{ (width ?? 30)/2 }}" cy="{{ (height ?? 30)/2 }}" r="{{ (width ?? 30)/2 }}" stroke="black" stroke-width="1" fill="{{ color ?? "white" }}" />
</svg>