{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}
    {% if 'ROLE_USER' in app.user.roles and app.user.roles|length == 1 %}
        {% include("dashboard//components//attendee_dash.html.twig") %}
    {% elseif  'ROLE_DEALER' in app.user.roles %}
        {% include("dashboard//components//attendee_dash.html.twig") %}
    {% elseif  'ROLE_CURATOR' in app.user.roles %}
        {% include("dashboard//components//curator_dash.html.twig") %}
    {% elseif 'ROLE_ADMIN' in app.user.roles %}
        {% include("dashboard//components//admin_dash.html.twig") %}
    {% endif %}

{% endblock %}
